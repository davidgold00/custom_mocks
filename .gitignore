node_modules

# build output
dist/
*.tsbuildinfo

# Vercel CLI local state (project link + pulled env vars)
.vercel/

# local env files (pulled via `vercel env pull`, contain real secrets)
.env*

# OS noise
.DS_Store

# unused local python env from an earlier data-conversion script
.venv/
